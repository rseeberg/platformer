{
  "name": "platformer-game",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "http-server -p 8080 -c-1",
    "dev": "npm run build && npm run serve"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^5.9.2"
  }
}
